{
  "name": "prova",
  "version": "0.0.1",
  "private": true,
  "description": "Cryptographic receipts for the agentic internet — behavior attestation layer for AI agents on Solana",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "dotenv -e .env.local -- turbo run build",
    "dev": "dotenv -e .env.local -- turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md,json,yaml}\" --ignore-path .gitignore",
    "typecheck": "turbo run typecheck",
    "test": "dotenv -e .env.local -- turbo run test",
    "test:e2e": "dotenv -e .env.local -- turbo run test:e2e",
    "clean": "turbo run clean && rimraf node_modules",
    "db:generate": "dotenv -e .env.local -- turbo run db:generate --filter=@prova/db",
    "db:migrate": "dotenv -e .env.local -- turbo run db:migrate --filter=@prova/db",
    "db:studio": "dotenv -e .env.local -- pnpm --filter=@prova/db db:studio",
    "ship": "vercel --prod -y"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.12",
    "dotenv-cli": "^11.0.0",
    "next": "^15.1.6",
    "prettier": "^3.4.2",
    "rimraf": "^6.0.1",
    "turbo": "^2.3.3",
    "typescript": "^5.7.3"
  }
}
