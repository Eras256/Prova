[workspace]
members = ["programs/prova-program"]
resolver = "2"

# Los profiles solo aplican desde la raíz del workspace (cargo los ignora en
# los miembros), por eso viven aquí y no en programs/prova-program/Cargo.toml.
[profile.release]
overflow-checks = true
